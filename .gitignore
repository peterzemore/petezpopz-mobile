node_modules/
.expo/
dist/
web-build/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
.env
.env.local
.DS_Store
expo-env.d.ts

# Native folders
/ios
/android
.env.local

# Separate, standalone git repo (created by `shopify app init`) for the
# checkout UI extension — not part of this repo's history.
checkout-rewards-extension/

# Contains a Vercel/EAS automation token — never commit.
APP automation token
